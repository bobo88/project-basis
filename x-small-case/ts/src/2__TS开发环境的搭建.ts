/**
 * 1. 全局安装 typescript
 * 2. 创建 ts 文件
 * 3. 使用 tsc 对 ts 文件进行编译： tsc xxx.ts
 * 4. 生成配置文件：tsc --init 生成tsconfig.json
 * 5. 自动编译： tsc --watch OR tsc -w
 * 6. 
 */
;(function(){
    const arr: number[] = [1, 2, 3, 3, 4]
})()